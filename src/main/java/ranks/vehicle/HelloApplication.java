package ranks.vehicle;

import ranks.vehicle.view.VehicleIdentificationSystemApp;

public class HelloApplication extends VehicleIdentificationSystemApp {
    public static void main(String[] args) {
        launch(args);
    }
}
